import { Activity } from "lucide-react";
import { getSeasonAnalytics } from "@/services/productionOperationsService";
export const dynamic="force-dynamic";
export default async function SeasonAnalyticsPage(){const rows=await getSeasonAnalytics();return <><div className="mb-7"><p className="label mb-2 text-neon">Season analytics</p><h1 className="text-3xl font-black md:text-4xl">Performance por competição</h1><p className="mt-2 text-sm text-zinc-500">Somente tips reais liquidadas como WON ou LOST.</p></div><section className="card overflow-hidden"><div className="overflow-x-auto"><table className="w-full min-w-[700px] text-left text-xs"><thead><tr className="border-b border-line text-[9px] uppercase text-zinc-600"><th className="px-5 py-3">Competição</th><th>ROI</th><th>Win Rate</th><th>Greens</th><th>Reds</th><th>Lucro</th><th>Entradas</th></tr></thead><tbody>{rows.length?rows.map(row=><tr key={row.competition} className="border-b border-line/60"><td className="px-5 py-4 font-black">{row.competition}</td><td className={row.roi>=0?"text-neon":"text-red-400"}>{row.roi.toFixed(2)}%</td><td>{row.winRate.toFixed(2)}%</td><td className="text-neon">{row.greens}</td><td className="text-red-400">{row.reds}</td><td>{row.profit.toFixed(2)}u</td><td>{row.entries}</td></tr>):<tr><td colSpan={7} className="px-5 py-12 text-center text-zinc-600"><Activity className="mx-auto mb-3"/>Ainda não há tips reais liquidadas para análise por competição.</td></tr>}</tbody></table></div></section><div className="mt-6 rounded-xl border border-line p-4 text-[11px] text-zinc-500">Resultados históricos não prometem lucro nem garantem green. Aposte com responsabilidade.</div></>}

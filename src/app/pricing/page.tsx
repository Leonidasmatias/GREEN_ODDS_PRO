import Link from "next/link";
import { PLAN_DEFINITIONS } from "@/services/subscriptionAccess";

export default function PricingPage() {
  return <main className="mx-auto max-w-6xl py-10"><p className="label mb-2 text-neon">Planos</p><h1 className="text-3xl font-black">Acesso comercial preparado</h1><p className="mt-2 max-w-2xl text-sm text-zinc-500">Pagamento fica para a proxima fase. Hoje os planos definem permissao e limites.</p><div className="mt-8 grid gap-4 md:grid-cols-3">{Object.values(PLAN_DEFINITIONS).map((plan) => <section key={plan.code} className="card p-6"><p className="label text-neon">{plan.code}</p><h2 className="mt-2 text-2xl font-black">{plan.name}</h2><p className="mt-2 min-h-12 text-sm text-zinc-500">{plan.description}</p><ul className="mt-5 space-y-2 text-xs text-zinc-400"><li>Picks/dia: {plan.maxPicksPerDay ?? "sem limite operacional"}</li><li>Radar: {plan.radarAccess ? "liberado" : "bloqueado"}</li><li>Odds do Dia: {plan.oddsAccess ? "liberado" : "bloqueado"}</li><li>Green AI Report: {plan.greenAiReportLevel}</li><li>Command Center: {plan.commandCenterLevel}</li></ul><Link href="/register" className="mt-6 block rounded-xl border border-line px-4 py-3 text-center text-xs font-black uppercase text-white">Comecar</Link></section>)}</div><div className="mt-6 rounded-xl border border-amber-400/20 bg-amber-400/[.05] p-4 text-xs text-amber-200">O Green Odds Pro nao garante lucro. Analises sao estatisticas e informativas. Apostas envolvem risco financeiro.</div></main>;
}

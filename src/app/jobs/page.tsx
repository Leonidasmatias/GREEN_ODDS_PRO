import { Activity, AlertTriangle, CheckCircle2, Clock3 } from "lucide-react";
import { getJobMonitor } from "@/services/schedulerService";
export const dynamic = "force-dynamic";
const date = (value: string | null) => value ? new Intl.DateTimeFormat("pt-BR", { dateStyle: "short", timeStyle: "medium" }).format(new Date(value)) : "—";
export default async function JobsPage() { const data = await getJobMonitor(); const cards=[{label:"Jobs executados",value:data.summary.executed,Icon:CheckCircle2},{label:"Pendentes",value:data.summary.pending,Icon:Clock3},{label:"Falhas",value:data.summary.failed,Icon:AlertTriangle},{label:"Tempo médio",value:`${data.summary.averageDurationMs} ms`,Icon:Activity}]; return <><div className="mb-7"><p className="label mb-2 text-neon">Production operations</p><h1 className="text-3xl font-black md:text-4xl">Job Monitor</h1><p className="mt-2 text-sm text-zinc-500">Execuções persistidas do scheduler e tempos reais de processamento.</p></div><div className="grid gap-4 md:grid-cols-4">{cards.map(({label,value,Icon})=><div className="card p-5" key={label}><Icon className="text-neon" size={17}/><p className="label mt-4">{label}</p><strong className="mt-2 block text-2xl">{value}</strong></div>)}</div><div className="card mt-4 p-4 text-xs text-zinc-500">Odds: {data.frequenciesMinutes.odds} min · Resultados: {data.frequenciesMinutes.results} min · Última sincronização: <b className="text-white">{date(data.summary.lastSync)}</b></div><section className="card mt-6 overflow-hidden"><div className="overflow-x-auto"><table className="w-full min-w-[850px] text-left text-xs"><thead><tr className="border-b border-line text-[9px] uppercase text-zinc-600"><th className="px-5 py-3">Job</th><th>Status</th><th>Agendado</th><th>Início</th><th>Fim</th><th>Duração</th><th>Mensagem</th></tr></thead><tbody>{data.runs.map((run)=><tr key={run.id} className="border-b border-line/60"><td className="px-5 py-4 font-black">{run.name}</td><td className={run.status==="FAILED"?"text-red-400":"text-neon"}>{run.status}</td><td>{date(run.scheduledAt)}</td><td>{date(run.startedAt)}</td><td>{date(run.completedAt)}</td><td>{run.durationMs??0} ms</td><td className="text-zinc-500">{run.message??"—"}</td></tr>)}</tbody></table></div></section><div className="mt-6 rounded-xl border border-line p-4 text-[11px] text-zinc-500">Operação e métricas não representam promessa de lucro ou garantia de green. Aposte com responsabilidade.</div></>; }
